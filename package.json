{
  "name": "pageask-extension",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build": "vite build && node scripts/copy-static.mjs",
    "check": "vite build"
  },
  "dependencies": {
    "@pixiv/three-vrm": "^3.4.0",
    "three": "^0.178.0"
  },
  "devDependencies": {
    "vite": "^7.1.5"
  }
}
